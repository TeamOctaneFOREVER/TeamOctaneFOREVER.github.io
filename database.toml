# To be worked on...

[repository]
name = "Diesel"


[[packages]]
name = "test"
version = "1.0.0"
authors = ["Jesse"]
url = "https://teamoctaneforever.github.io/pkgs/test.txt"

[[packages]]
name = "bigtest"
version = "1.0.0"
authors = ["Jesse"]
url = "https://teamoctaneforever.github.io/pkgs/megabyte.txt"
dependencies = ["test"]  # Specify dependencies as a list

[[packages]]
name = "multi-test"
version = "2.0.0"
authors = ["Jesse"]
url = ""  # Add the URL for the package
dependencies = ["test", "bigtest"]  # Specify multiple dependencies as a list
